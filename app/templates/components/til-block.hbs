{{yield}}

<li class="notepad" style="margin-bottom:5px">
  <button {{action 'vote' til}}>Vote!</button>
  {{til.votes.length}}
  {{markdown-to-html til.description }}
  <span id="author-small">{{til.author.email}}</span>
</li>
